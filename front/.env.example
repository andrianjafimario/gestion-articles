# Frontend (Vite) environment variables
# Copy this file to `front/.env` and adjust values for your environment.

# Backend base URL (without trailing /api, code appends /api automatically)
VITE_API_BASE_URL="http://localhost:5000"
